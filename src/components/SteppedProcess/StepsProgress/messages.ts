import { defineMessages } from "react-intl";

export default defineMessages({
  stepAccessDisabled: {
    id: "app.layouts.SteppedProcess.stepAccessDisabled",
    defaultMessage: "Only authors of this objective can perform this action",
  },
});
